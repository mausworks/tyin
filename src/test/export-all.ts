import storeHook from "../hook";
import createStore from "../store";
import extend from "../extend";
import arrayAPI from "../plugin-array";
import objectAPI from "../plugin-object";
import persist from "../plugin-persist";

// This is here to measure the bundle size of the library.
export { storeHook, createStore, extend, arrayAPI, objectAPI, persist };
